#include<stdio.h>
int main()
{
    double dA = 10.44;
    double dB = 11.55;
    double dC = 12.66;

    double Add = 0;

    Add = dA + dB + dC;

    printf("Addition of Double Integers are = %lf\n",Add);

    return 0;
}